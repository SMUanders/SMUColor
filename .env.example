# SMU Color — miljøvariabler (frontend)
# Kopiér til .env.local og udfyld. .env.local er gitignored.
#
# Peger på det DELTE SMU Supabase-projekt (samme som SMU OS/Wiki/Tid).
# Kun anon key og url — aldrig service role key i frontend.
#
# Uden disse to kører appen i LOKAL DEV-TILSTAND (localStorage + seed-data).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
